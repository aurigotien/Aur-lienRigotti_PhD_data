### Simulation parameters ###

	## Simulation parameters ##
	
		dimension	2 # 2D simulation
		boundary	p p p # Bondary condtion, p periodic conditions, fm fixed/mobile to shrink the box
		atom_style   	sphere # Spherical particles, must define diameters and weigth for every particles create
		atom_modify 	map yes
		newton       	on # off = no action/reaction
		comm_modify  	vel yes 
		units        	si # scientific units used 
    
    	## Simulation variables ## 

		# Constant to be modified by the python routine 
		#variable_D_MIN#
		#variable_D_MAX#
		#variable_N_GRAINS#
		#variable_LINI#
		#variable_RHO#
		#variable_VISC#  
		variable 	MU_INI equal 0 # phi ~ 0.8 => mu = 0
		
		# Grains parameters
		variable 	K_N equal 1.1E9 #true value = 1.1E9
		variable 	K_T equal 9.05E8 #true value = 9.05E8
		variable 	GAMMA_N equal 14.0
		variable 	GAMMA_T equal 12.0
	
		# Monitoring variable 
		variable 	D_AVE equal "(v_D_MIN + v_D_MAX)/2" # average diameter of the grains 
		
	## Geometry ##
        
		region 	boite block 0 ${L_INI} 0 ${L_INI} -0.5 0.5 units box # Create a region "boite" L_INI x L_INI x 1
		box          	tilt large # allow large deformation of the box A(xy) > 45°
		
		create_box   	2 boite # Create the simulation box "boite" with 2 type of grains
		change_box   	all triclinic # set periodic boundary conditions
	
	## Grain insertion ##  
	      
		read_data 	rand_config.lj add merge # add the rand_config file with random distribution of grains in space / diameter
		set 		group all density/disc ${RHO} # set a density in kg/m^2 (2D model)
		
		# Preparation protocole grains properties
		
		# Protocole 1
		pair_style   	gran/hertz/history ${K_N} 0.0 ${GAMMA_N} 0.0 ${MU_INI} 0 # Frictionless hertzian contacts 
		
		# Protocole 2
		#pair_style   	gran/hertz/history ${K_N} ${K_T} ${GAMMA_T} ${GAMMA_N} ${MU_INI} 1 # Frictionless hertzian contacts
		
		pair_coeff	* *
		neighbor     	80.0 bin # Update frequency of the particle list skin = 80 m
	
	## Diameters groups ## 
		
		group 		G_DMIN type 1
		group 		G_DMAX type 2 

### End of initialisation step ###
